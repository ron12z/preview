---

---

<div class="escalation">
	<button id="resetBtn">Reset</button>

	<p id="escalation-result">Escalator v2</p>
</div>

<style>
	.escalation {
		background: rgb(101, 165, 170);
		padding: 1rem;
		display: flex;
		gap: 0.5rem;
		position: relative;
		cursor: not-allowed;
	}

	#escalation-result {
		width: 100%;
		min-height: 100%;
	}

	#escalation-result::after {
		content: " -Press C or Click to copy.";
		color: black;
		opacity: 0.5;
		z-index: 0;
	}

	#escalation-result.no-content::after {
		content: "";
	}

	button {
		padding: 1rem;
		border: 1px solid black;
		height: inherit;
		width: min(5rem, 30%);
		background: lightcoral;
		border-radius: 0.3rem;
		align-self: center;
		z-index: 10;

		&:hover {
			background: red;
		}

		&:active {
			position: relative;
			top: 3px;
		}
	}
</style>

<script>
	import { resetEscalation } from "../scripts/escalation";

	document.querySelector("#resetBtn")!.addEventListener("click", () => {
		const allChecked = document.querySelectorAll(".checked");
		const allInputs = document.querySelectorAll("input");
		const allAdditionalInputs = document.querySelectorAll(".additional-input");
		const allInputFromAddmore = document.querySelectorAll(".from-addMore");

		allChecked.forEach((i) => i.classList.remove("checked"));
		allInputs.forEach((i) => (i.value = ""));
		allAdditionalInputs.forEach(
			(i) => ((i as HTMLElement).style.display = "none")
		);
		allInputFromAddmore.forEach((i) => i.remove());

		resetEscalation();
	});
</script>
